worker_processes 4;

events { 
    worker_connections 1024; 
}

http {
    server {
        server_name localhost;

        listen 80;

        location /api {
            proxy_pass http://choice.webapi:80/api;
        }

        location /api/Auth {
            proxy_pass http://choice.authentication:8081/api/Auth;
        }
    }
}
