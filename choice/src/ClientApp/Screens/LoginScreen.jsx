import React from "react";
import {
  Image,
  SafeAreaView
} from 'react-native';

export default function LoginScreen() {
    return (
        <SafeAreaView>
            <Image source={{uri: 'choice-logo'}}/>
        </SafeAreaView>
    )
}