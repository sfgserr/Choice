import {createBottomTabNavigator} from '@react-navigation/bottom-tabs';
import {ClientTabProps} from '../../../types/NavigationTypes.ts';

export const ClientTab = createBottomTabNavigator<ClientTabProps>();
