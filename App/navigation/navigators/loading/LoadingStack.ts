import {createNativeStackNavigator} from '@react-navigation/native-stack';
import {LoadingStackProps} from '../../../types/NavigationTypes.ts';

export const LoadingStack = createNativeStackNavigator<LoadingStackProps>();
