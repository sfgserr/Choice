import {createNativeStackNavigator} from '@react-navigation/native-stack';
import {AdminStackProps} from '../../../types/NavigationTypes.ts';

export const AdminStack = createNativeStackNavigator<AdminStackProps>();
