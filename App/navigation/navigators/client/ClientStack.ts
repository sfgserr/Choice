import {createNativeStackNavigator} from '@react-navigation/native-stack';
import {ClientStackProps} from '../../../types/NavigationTypes.ts';

export const ClientStack = createNativeStackNavigator<ClientStackProps>();
