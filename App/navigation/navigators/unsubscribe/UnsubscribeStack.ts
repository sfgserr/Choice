import {createNativeStackNavigator} from '@react-navigation/native-stack';
import {UnsubscribeStackProps} from '../../../types/NavigationTypes.ts';

export const UnsubscribeStack = createNativeStackNavigator<UnsubscribeStackProps>();
