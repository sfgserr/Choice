import {createNativeStackNavigator} from '@react-navigation/native-stack';
import {UserStackProps} from '../../../types/NavigationTypes.ts';

export const UserStack = createNativeStackNavigator<UserStackProps>();
