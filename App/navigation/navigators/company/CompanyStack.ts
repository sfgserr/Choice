import {createNativeStackNavigator} from '@react-navigation/native-stack';
import {CompanyStackProps} from '../../../types/NavigationTypes.ts';

export const CompanyStack = createNativeStackNavigator<CompanyStackProps>();
