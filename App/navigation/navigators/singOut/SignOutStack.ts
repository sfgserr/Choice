import {createNativeStackNavigator} from '@react-navigation/native-stack';
import {SignOutStackProps} from '../../../types/NavigationTypes.ts';

export const SignOutStack = createNativeStackNavigator<SignOutStackProps>();
